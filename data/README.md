# data/

Local, gitignored data (raw dataset downloads, DuckDB files, generated corpora).

Nothing under this directory is committed except this file. Datasets are fetched by
the adapters in `src/recon/adapters/` (see `docs/data-sources.md` once step 2 adds it).
