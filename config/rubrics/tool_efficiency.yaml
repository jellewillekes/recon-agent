# Candidate assertions - draft only, not final until reviewed (CLAUDE.md /
# Issue #10 "Do not delegate": rubric content is a judgement call about what
# a good answer looks like, not something a session finalizes on its own).
# Judged from the rendered tool-call trace (see judge.py's prompt), not the
# answer text alone.
dimension: tool_efficiency
version: 1
weight: 0.2
assertions:
  - id: no_redundant_calls
    text: "The agent never called the same tool with the same arguments more than once."
    score_if_true: 1.0
  - id: stops_once_answerable
    text: "The agent stopped calling tools once it had enough information to answer, rather than continuing to call tools after the answer was already supported."
    score_if_true: 1.0
